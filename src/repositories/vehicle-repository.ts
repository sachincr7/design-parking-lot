import { Vehicle } from "../modules/vehicle";

export class VehicleRepository {
  findVehicleById (vehicleNumber: string): Vehicle | null {
    return null;
  }

  save (vehicle: Vehicle): Vehicle | null {
    return null;
  }
}